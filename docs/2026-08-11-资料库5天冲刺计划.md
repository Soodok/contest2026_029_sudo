# 《资料库》参赛实施计划（格式先行 · 5 天冲刺）

> 队伍 029「sudo」· 应用名：**资料库** · 包名：`com.vela.databank`
> 冲刺窗口：**2026-08-12 ~ 08-16（5 天）** · 基座：`historyband9`（已验证 v1.3.0）
> 前置定稿：**《数据格式规范 v5》**（docs/2026-08-11-数据格式规范v5.md）——本计划严格按该规范执行

## 一、总体目标

5 天内完成《资料库》可提交作品：**v5 通用数据格式 + 引擎适配 + 读取端动态渲染 + 多知识库 + 健康三卡 + release.rpk**。

## 一·五、人员分工（核心开发 A 全包，B 只做"AI 供给"任务）

| 角色 | 定位 | 任务 |
|---|---|---|
| **A（主程=用户）** | 全部代码开发 | 引擎适配、数据工具改造、读取端、健康模块、打包、Git |
| **B（队友）** | **只会用 AI → 用 AI 干"供给型"任务** | ① 用 AI 生成数据（词典/单位/百科，照 prompt 让 AI 输出，复制存 txt）；② 模拟器测试走查（截图发 A）；③ 每天 opencode 记 AI 日志（满足大赛 AI 使用分）；④ 材料辅助（README/视频录屏） |

> **B 的核心价值**：数据是引擎的"燃料"。B 用 AI 生成数据（A 验证格式即可），帮 A 省掉最耗时的数据收集环节；同时 B 的 opencode 对话天然累积成大赛所需的 AI 日志。
> **B 铁律**：绝不碰代码文件；不会的操作问 A 或让 AI 帮忙，不硬来。

## 二、核心主线（格式先行，顺序不可颠倒）

```
数据格式 v5（已定稿）→ 引擎适配 → 数据工具适配 → 数据生成 → 读取端适配 → 全链路验证 → 打包提交
```

**v5 格式速记**（详情见规范文档）：
```
Map：     ID | [年份] | 简约1 | 简约2 | 简约3
Detail：  ID | [年份] | 关键词串 | 详情1..N | [图片]
meta：    dataset + fields(map/detail/keyword/hiddenKeyword/image) + year(rules) + chunks/maps
```
- 字段角色：keyword / hiddenKeyword / map(≤3) / detail(N 任意) / image(仅详情)
- 存储唯一性：map 与 detail 互斥、图片仅 Detail
- 无筛选（附录 A 可扩展）

---

## 三、引擎适配方案（SearchEngine.js，A 主程，Day1-2）

引擎当前为 v3.4（硬编码 v4 格式），需改为**数据结构零假设、依赖 meta 配置**。逐函数改造：

### 3.1 `_loadMeta()`（核心）
- **现状**：读 v4 meta（totalCount/regionList/categoryList/chunks/maps/bucketSize）
- **改为**：读 v5 meta：
  ```js
  this.dataset   = meta.dataset || {}                  // 数据集信息
  this.fieldMap   = meta.fields.map || []              // 简约字段配置
  this.fieldDetail= meta.fields.detail || []           // 详情字段配置
  this.fieldKw    = meta.fields.keyword || []          // 关键词字段
  this.fieldHidden= meta.fields.hiddenKeyword || []
  this.fieldImage = meta.fields.image || []            // 图片字段
  this.yearCfg    = meta.year || { enable:false }      // 年份配置
  this.mapCols    = 1 + (yearCfg.enable?1:0) + fieldMap.length   // Map 列数
  this.hasImage   = fieldImage.length > 0
  ```
- 兼容：若 meta 无 `formatVersion`（旧 v4），走旧解析路径（保留旧逻辑，便于回退）

### 3.2 `_loadMapFromFile(mapId)`（动态列）
- **现状**：硬编码 4 列 → `ids/years/regionIds/titles`
- **改为**：
  ```js
  var cols = this.mapCols   // ID + [年份] + 简约N
  ids.push(parseInt(parts[0],10))
  if (yearCfg.enable) years.push(parseInt(parts[1],10))
  displayVals.push(parts.slice(1+(yearCfg.enable?1:0), cols))   // 简约列数组
  ```
- 存储：`mapData = { ids, years?, displayVals: [ [v1,v2], ... ] }`
- 说明：`displayVals` 每项是该记录简约字段值数组（长度=fieldMap.length）

### 3.3 Detail 解析（`getDoc` / `getDocSync`）（按 N 切片）
- **现状**：固定列取 年份/地区/分类/标题/关键词/原因/影响
- **改为**（统一解析函数 `_parseDetailLine(parts)`）：
  ```js
  var idx = 0
  var doc = { id: parseInt(parts[idx++],10) }
  if (yearCfg.enable) doc.year = parseInt(parts[idx++],10)
  if (fieldKw.length > 0) doc.keywords = (parts[idx++] || '').split(',').filter(Boolean)
  doc.details = parts.slice(idx, idx + fieldDetail.length)   // N 列详情
  idx += fieldDetail.length
  if (hasImage) doc.image = parts[idx] || null               // 末尾图片路径
  return doc
  ```
- 字段名映射：`detailFields[i].name` 对应 `doc.details[i]`（详情页标签）

### 3.4 `search` 展示（卡片数据）
- **现状**：regionMap 映射地区名 + title
- **改为**：卡片直接用 `mapData.displayVals`：
  ```js
  var dv = mapData.displayVals[id] || []
  return { id, year, title: dv[0] || '', subtitle: dv[1] || '', yearDisplay: fmtYear(year) }
  ```

### 3.5 年份显示 `_formatYearDisplay`
- **现状**：固定规则
- **改为**：按 `yearCfg.rules` 匹配（`min≤value≤max → display.replace('{value}',value)`），未命中用 `defaultDisplay`

### 3.6 初始化路径（splash 侧不变）
- `engine.init(basePath, cb)` / `initLight(basePath)` 签名不变，仅内部解析升级

**验收（Day2 末）**：用词典库 2 行示例（规范 6.1）手工构造 v5 数据，`getDoc(0)` 返回 `{keywords:['苹果','apple'], details:['一种蔷薇科水果',...]}`，`search('苹果')` 命中 ID0 且卡片显示"苹果 名词"。

---

## 四、数据工具（New PL）适配要求（A 主程 + B 协助，Day2-3）

1. **meta 输出对齐 v5**：去掉 `filter`，输出 `dataset/fields(map/detail/keyword/hiddenKeyword/image)/year/chunks/maps`（`formatVersion:5`）
2. **Map 固定列**：`ID|[年份]|简约1..3`（无图片列）
3. **Detail 固定段**：`ID|[年份]|关键词串|详情1..N|[图片]`，空值保留占位
4. **UI 简化**：字段列 → 勾选角色（关键词/隐形/简约/详情/图片）+ 填列名 + 填权重（模板式，替代原复杂独立判断）
5. 输出文件清单：meta.json + map_*/detail_* + block_* + year_index(可选) + images/

**B 的任务（用 AI 生成数据，照抄下面 prompt 即可）**：

在任意 AI 对话工具（opencode / 网页 AI）中输入模板，让 AI 输出数据文本，复制保存为 `e:\vela Project\data\<库名>.txt`：

```
【词典库 prompt 模板】生成一份中文常用词语词典数据，要求：
1. 每行格式：词语|词性|英文别名|释义|例句|用法说明
2. 严格用竖线 | 分隔，字段内不要出现竖线
3. 共 2000 行，覆盖日常用语、学习、网络常用词
4. 直接输出文本，不要多余解释
```

```
【单位库 prompt 模板】生成一份单位换算数据，要求：
1. 每行格式：单位|类型|英文全称|换算关系|说明
2. 严格用竖线 | 分隔，共 2000 行
3. 覆盖长度、重量、容量、时间、温度等类型
4. 直接输出文本
```

```
【百科库 prompt 模板】生成一份百科速览数据，要求：
1. 每行格式：词条|领域|别名|一句话定义|详细说明
2. 严格用竖线 | 分隔，共 2000 行
3. 覆盖自然、科技、人文、生活常识
4. 直接输出文本
```

> 提示：AI 一次输出不了 2000 行就分几次生成（每次 500 行），合并到一个文件。**A 只需验证格式对、行数够**。

---

## 五、读取端适配（A 主程，Day3-4）

| 文件 | 改造点 |
|---|---|
| `splash.ux` | 读取 `meta.dataset.name` 显示加载标题；`mountToGlobal` 挂载 `getDoc` 返回新结构 |
| `index.ux` | 移除筛选标签行；结果预览卡显示 `title/subtitle`（displayVals 前 2 列） |
| `result.ux` | 列表项显示 displayVals；分页不变 |
| `detail.ux` | **核心**：按 `meta.fields.detail` 动态渲染区块（`for` 循环 name=标签、value=内容）；若 `doc.image` 存在渲染 `<image src="{{imgPath}}">`；关键词标签显示 |
| `settings.ux` | 知识库切换（读各库 meta.dataset.name）；主题开关保留 |

---

## 六、5 天每日任务表（A 主程 / B 测试资料）

### Day 1（08-12）：引擎 v5 适配（上）
- **A**：① `_loadMeta` v5 解析（fields/year/dataset）；② `_loadMapFromFile` 动态列；③ 用词典 2 行示例构造 v5 数据验证 meta+Map 解析
- **B**：① 建模拟器（vela-miwear-watch-5.0）；② 确认 opencode 日志可用；③ 用**词典库 prompt 模板**让 AI 生成数据，存 `data/词典.txt`
- **验收**：meta.json + map_0.txt 解析通过（日志无错、字段数正确）

### Day 2（08-13）：引擎 v5 适配（下）+ 数据工具
- **A**：④ Detail 解析（_parseDetailLine 按 N 切片+图片）；⑤ search 展示 displayVals；⑥ 年份 rules；⑦ New PL 输出对齐 v5（去 filter/固定列）
- **B**：① 继续生成词典数据至 ≥2000 行（分 4 次各 500 行合并）；② 检查格式（竖线分隔、无多余列）
- **验收**：`getDoc(0)` 返回 {keywords, details, image} 正确；词典数据文件已备好

### Day 3（08-14）：读取端适配 + 多知识库
- **A**：① detail.ux 动态渲染（N 详情 + 图片 + 关键词）；② index/result 显示 displayVals；③ 用 B 的词典数据生成 v5 数据包并接入
- **B**：① 用**单位库 prompt 模板**生成单位数据 ≥2000 行；② 模拟器回归：切库→搜索→结果→详情全链（截图发 A）
- **验收**：双库可切可搜，详情动态渲染正确

### Day 4（08-15）：百科库 + 健康模块 + 全链路
- **A**：① 百科库接入（N=4 详情验证高自定义）；② service.health 三卡（manifest 声明+订阅）；③ 全功能回归修 bug
- **B**：① 用**百科库 prompt 模板**生成百科数据 ≥2000 行；② 健康卡测试（截图 3 张）；③ 全流程回归输出问题清单
- **验收**：4 库可用；健康三卡刷新；核心功能无崩溃

### Day 5（08-16）：打包 + 材料 + 提交
- **A**：① `npm run build/release` → rpk；② adb 推送模拟器验证；③ 整合专属仓 PR
- **B**：① 录 ≤5min 视频；② 确认 AI 日志 ≥5 次会话并 validate-log；③ README 初稿（让 AI 辅助写）
- **验收**：release.rpk 可装可跑；材料齐备

---

## 七、里程碑与验收检查点

| 里程碑 | 日期 | 检查点 |
|---|---|---|
| 格式定稿 | 08-11（已完成） | 规范 v5 文档已 commit |
| 引擎 v5 | 08-13 | getDoc/search 对新结构正确（单测示例数据） |
| 多库+读取端 | 08-14 | 4 库切库搜索、详情动态渲染 |
| 健康+全链 | 08-15 | 健康三卡 + 无崩溃回归 |
| 提交 | 08-16 | release.rpk + 材料齐 |

## 八、风险与兜底

| 风险 | 兜底 |
|---|---|
| 引擎改动破坏旧功能 | 保留 v4 兼容分支（meta 无 formatVersion 走旧逻辑），可随时回退 |
| New PL 改造量大 | 先保证"能输出 v5 数据"即可（B 用历史库验证），UI 简化可后置 |
| 数据不足 2000 | 先 1000 条标注"演示版" |
| rpk 失败 | 提交源码 + 构建说明 |
| 健康模块异常 | 用 vela-watch-5 镜像 + 静态卡降级 |
