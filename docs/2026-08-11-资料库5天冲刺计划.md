# 《资料库》参赛实施计划（5 天冲刺版）

> 队伍 029「sudo」· 应用名：**资料库** · 包名：`com.vela.databank`
> 冲刺窗口：**2026-08-12 ~ 08-16（5 天）** · 目标：核心功能可演示、release.rpk 可提交
> 基座：`historyband9`（已验证 v1.3.0，含完整搜索链/输入法/2533 条数据）

## 一、冲刺原则（先砍再做）

| 类别 | 内容 | 处置 |
|---|---|---|
| **保留（核心）** | 去激活、品牌"资料库"、圆屏搜索链、多知识库×2、健康三卡、打包、README、AI 日志 | 全部 5 天内完成 |
| **降级** | AI（velaclaw） | 仅代码预留 `ai.js`，不做真实验证 |
| **砍掉** | 功能直达页（番茄钟等）、方屏适配、演示视频精修、多知识库×4 | 全部不做/极简（有富余再做） |

## 二、每日任务表（A 主程 / B 测试资料，按小时粒度）

### Day 1（08-12）环境 + 基座清理 + 圆屏跑通

| 时段 | A（主程）具体动作 | B（测试/资料）具体动作 |
|---|---|---|
| 上午 | ① AIoT IDE 打开 `e:\vela Project\historyband9`；② 跑一次基线确认可进首页；③ 删 `src/pages/activate/` 目录 + manifest router 删 activate 行 | ① AIoT IDE 建模拟器：镜像 `vela-miwear-watch-5.0(开发者大赛)`，命名「资料库-圆屏」；② Git Bash 在专属仓跑 `opencode run "Day1日志"` 确认日志出现 |
| 下午 | ④ splash.ux `finish()`：删 `activate_key` 整段 → `router.replace({uri:'/pages/index'})`；⑤ index.ux 删 `trial-banner`/`checkTrial()`/`isTrial`；⑥ manifest 改 `package:"com.vela.databank"`、`name:"资料库"`、`designWidth:480` | ③ 找开源词典词表，整理成 `标题\|释义` 文本，≥2000 条，存 `e:\vela Project\data\词典.txt`；④ 通读页面把"万象史"旧词列修改清单发给 A |
| **验收** | 圆屏模拟器启动 → 直接进首页（无激活/试用），标题"资料库"，首页布局在 480 下不错位 | 数据文件 ≥2000 行；日志文件已出现 |

### Day 2（08-13）核心搜索链圆屏跑通 + 数据供给

| 时段 | A 具体动作 | B 具体动作 |
|---|---|---|
| 上午 | ① 圆屏下验证搜索链：输入"秦始皇"→候选→预览3条→结果→详情→返回；② InputMethod `screentype` 确认圆屏形态、键盘不遮输入框 | ① 按脚本测试搜索链并截图（输入法候选/结果/详情各 1 张）发 A；② 继续整理「单位换算」数据 ≥2000 条存 `e:\vela Project\data\单位.txt` |
| 下午 | ③ 修 B 反馈的圆屏布局问题（键盘遮挡/错位）；④ 检查 detail 收藏持久化 | ③ 测试收藏：收藏→关应用→重开→确认还在 |
| **验收** | 搜索全链 0.5~1s 无崩溃；圆屏布局正常 | 2 份数据文件齐（词典/单位各 ≥2000） |

### Day 3（08-14）多知识库 + 健康模块启动

| 时段 | A 具体动作 | B 具体动作 |
|---|---|---|
| 上午 | ① 浏览器开 `SearchEngine/tools/splitData.html`，用 B 的词典/单位数据生成数据包 → `src/common/词典/`、`src/common/单位/`（确认 meta.txt totalCount≥2000）；② 设置页加"知识库选择"（历史/词典/单位，storage key `currentDB`），splash 按所选库 init | ① 词典库搜索测试："苹果"→释义正确；② 提供"百科速览"数据 ≥2000 条（若时间够） |
| 下午 | ③ manifest 加 `service.health`（features+permissions+background）；④ 新建健康页或首页健康卡：`getRecentSamples` 补帧 + `subscribeSample` 订阅心率/血氧/压力 | ③ 逐库回归搜索（历史"秦始皇"/词典"苹果"/单位"摄氏度"）并记录耗时 |
| **验收** | 3 库可切换可搜索；健康三卡模拟器 1Hz 刷新 | 各库搜索正常，耗时记录在表 |

### Day 4（08-15）健康融合 + 打磨 + AI 预留

| 时段 | A 具体动作 | B 具体动作 |
|---|---|---|
| 上午 | ① 健康数据落 storage，搜索"压力"→ 实时压力值卡＋知识卡；② 建 `src/common/ai.js` 预留 velaclaw（无环境 catch 降级），设置页加"AI 增强"开关 | ① 健康页截图 3 张（间隔 5s）确认数值变化；② 搜"压力"确认融合卡出现 |
| 下午 | ③ 全功能回归：三库搜索/健康/收藏/切主题；修 bug；④ 圆屏布局精修（安全区 padding、字体） | ③ 全流程回归测试，输出最终问题清单；④ README 初稿（简介/运行方式/截图占位） |
| **验收** | 全部核心功能可演示、无崩溃 | 问题清单清零或标记；README 初稿完成 |

### Day 5（08-16）打包 + 材料 + 提交

| 时段 | A 具体动作 | B 具体动作 |
|---|---|---|
| 上午 | ① Git Bash 进工程根 `npm run build` → `npm run release`（配签名）→ 得 `dist/com.vela.databank.release.rpk`；② rpk 解压 → `adb push` 到模拟器 `/data/app/com.vela.databank/` → 串口 `vapp hap://app/com.vela.databank` 完整跑一遍 | ① 录屏 ≤5min（启动/三库搜索/健康卡/离线演示）；② `git add logs/` + 跑 `validate-log.py` |
| 下午 | ③ 整合代码到专属仓 `quickapp/`（+manifest linkfile）→ fork+PR 自行合入；④ 收尾提交 AI_CONTEXT | ③ README 定稿（补性能数据/截图）；④ 确认日志 ≥5 次会话且校验通过 |
| **验收** | release.rpk 可装可跑；代码已 PR 合入专属仓 | 材料齐：README+视频+日志 |

## 三、5 天里程碑（每个 Day 结束 A+B 互验）

```
Day1 基座无激活、圆屏直进首页  →  Day2 搜索全链圆屏可用  →  Day3 三库可搜+健康三卡
→  Day4 健康融合、功能全绿  →  Day5 release.rpk 提交入库
```

## 四、被砍任务（开学后如有时间再补，不阻塞提交）

| 任务 | 说明 |
|---|---|
| AI（velaclaw）真实验证 | 已代码预留 `ai.js`，无 Ubuntu 环境不阻塞 |
| 功能直达页 | 不做，删除 funcIndex 计划 |
| 方屏/跑道屏适配 | 只保证圆屏；方屏若错位可接受（初赛模拟器演示圆屏） |
| 多知识库×4 | 只做 3 个（历史+词典+单位），百科有富余再做 |
| 演示视频精修 | 简单录屏即可 |

## 五、每日必做（贯穿 5 天）

- **B 每天**：`opencode run` 记录 1 次 AI 对话（当天任务相关），截图 3 张以上发 A
- **A 每天**：结束时把当日改动整理一句同步给 B；有 git 就 commit 一次
- **两人**：当天验收不过 → 当天加班修完，不拖到明天

## 六、风险与快速兜底

| 风险 | 兜底 |
|---|---|
| 圆屏模拟器运行异常 | 用 `vela-watch-5` 普通镜像替代（少健康数据，健康模块演示降级为静态卡） |
| 数据收集不够 | 词典先做 1000 条（演示够用），验收标注"演示版" |
| rpk 打包失败 | 直接提交源码工程 + 说明构建步骤（评委可自行 build） |
| 5 天超时 | 砍健康融合（保留三卡），砍掉的部分写入 README"后续规划" |
