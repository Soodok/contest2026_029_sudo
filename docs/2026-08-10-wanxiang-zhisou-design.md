# 万象智搜 —— 腕上统一智能搜索应用 设计文档

> 2026 首届 openvela AI 硬件开发者大赛 · 队伍 029「sudo」
> 赛道：手表应用创新（快应用）
> 版本：v0.1（设计草案，待评审）
> 日期：2026-08-10

> **历史文档说明（2026-09-18 补注）**：本稿为项目初期的设计草案（当时的作品名与范围与最终提交的《资料库》不同），保留原始推演过程。
> 文中性能与验证口径已按事后事实校正：**全部功能与性能实测均在 openvela 官方模拟器完成**；小米手环 9 上仅完成 rpk 安装与应用启动验证，真机全功能回归未完成。

---

## 一、项目背景与目标

### 1.1 背景

队伍拥有自研的「万象史搜索引擎 v3.4」（纯 JS 实现，专为 Vela 手表/手环设计的离线轻量级全文检索引擎），已在 openvela 手表模拟器上完成验证（真机全功能回归未完成）。本次参赛将其迁移至 openvela 快应用环境，并融合官方系统能力（健康数据、端侧 AI Agent），打造一款贴合手表真实场景的腕上搜索应用。

### 1.2 核心指标（真实承诺，杜绝夸大）

| 指标 | 数值 | 说明 |
|---|---|---|
| 检索延迟 | 0.5~1 秒（上限约 2 秒） | 模拟器实测数据（真机未压测） |
| 索引容量 | 40~50 万关键字（理论 100 万） | 按单字/词切分，全文子串检索 |
| 数据覆盖 | 18 万条记录（已验证） | 历史大事记等知识库 |
| 引擎内存 | < 158KB（LRU 恒定） | 2 Block + 6 Map 缓存 |
| 启动 | 首次 10-20 秒 / 二次 1-2 秒 | 索引构建与缓存恢复 |

### 1.3 目标

1. 完成一款可演示、可提交的 openvela 快应用作品（图形 + AI + 健康数据三能力落地）。
2. 充分发挥自研引擎的差异化技术价值（离线、极速、大数据量、隐私）。
3. 融合官方能力，满足「技术难度 / 产品创新 / 完整度 / AI 开发」评分维度。

---

## 二、产品定位

**《万象智搜》—— 腕上统一智能搜索**
一个搜索入口，搜遍「知识 + 健康 + 系统」三大数据域，0.5~1 秒离线给出短答案卡片。

- 核心价值主张：「抬腕即知」—— 知识查询、健康自省、功能直达，一个搜索框全搞定。
- 差异化叙事：**AI 负责听懂你，引擎负责答得准；没网时，引擎独扛。**

---

## 三、需求与技术点对照表（每个功能点的落地依据）

| # | 功能需求 | 技术实现 | 可行性 | 风险/备注 |
|---|---|---|---|---|
| 1 | 极速离线全文检索 | SearchEngine.js v3.4（哈希桶索引 + 分块 + LRU），`@system.file` 读数据、`@system.storage` 缓存 | ✅ 已适配快应用环境（SplashPage.ux 验证） | 数据文件需用 tools/splitData.html 生成 |
| 2 | 多知识库（历史/词典/百科/单位） | 引擎自动识别数据包格式（meta.txt 声明 + 文件结构） | ✅ 现有能力 | 各知识库数据源需整理/采集 |
| 3 | 实时健康数据（心率/血氧/压力） | `@service.health`：getRecentSamples / subscribeSample；manifest 声明 features + permissions | ✅ 官方 API；AIoT IDE 镜像 `vela-miwear-watch-5.0(开发者大赛)` 内置 31 天真人数据回放 | 需 AIoT IDE 插件 aiot-core/aiot-emulator ≥ 1.7.22 |
| 4 | 健康日志检索（"昨晚心率"） | 订阅数据按时间戳落盘（`@system.storage`/文件），构造时间维度索引 | ✅ 数据量小，纯 JS 可行 | 首次需先运行采集数据 |
| 5 | 端侧 AI 问答（velaclaw） | `import velaclaw from '@system.velaclaw'`；`velaclaw.ask({query})`；goldfish 模拟器需开启 CONFIG_FEATURE_SYSTEM_VELACLAW 等配置 + 配置 MiMo Token | ✅ 官方教程完整 | 依赖 openvela goldfish 模拟器环境（需 WSL）；AIoT IDE 模拟器不支持 |
| 6 | 双引擎路由（先本地后云端） | 本地命中 → 直出；未命中/开放问题 → velaclaw（携带本地检索结果作 RAG 依据）；断网 → 本地模糊匹配兜底 | ✅ 逻辑层纯 JS | 需处理 AI 超时/失败降级 |
| 7 | 系统功能直达（搜"番茄"→番茄钟） | 关键词→路由映射表 + `@system.router` 跳转 | ✅ 纯 JS，无系统依赖 | — |
| 8 | 搜索建议/联想 | 引擎子串匹配 + 前缀匹配能力实现实时联想 | ✅ 现有能力 | — |
| 9 | 语音输入 | 视系统能力：优先调研 openvela ASR/ai_agent Voice 通道；不可用则键盘输入兜底 | ⚠️ 待调研 | 标注为可选增强，非核心依赖 |
| 10 | 数据包导入 | 模拟器环境用 adb push 模拟导入流程 | ✅ 可行 | 蓝牙导入链路设计已实现，待真机对端联调；初赛用推送替代 |

---

## 四、系统架构

```
┌───────────────────────────────────────────────────────────────┐
│                     应用层（快应用 UI）                        │
│  首页(搜索框+健康三卡) / 结果页 / 详情操作页 / 健康日志 / 设置   │
└──────────────────────────────┬────────────────────────────────┘
                               │
┌──────────────────────────────▼────────────────────────────────┐
│                    搜索路由层（纯 JS）                          │
│  输入 → 意图分类：知识查询 / 健康查询 / 功能直达 / 开放问题      │
│         │         │          │             │                   │
│         ▼         ▼          ▼             ▼                   │
│   本地引擎   健康适配器   路由映射表     velaclaw(AI)            │
│   (全文索引)  (实时+日志)  (功能直达)     (联网增强)             │
└──────────────────────────────┬────────────────────────────────┘
                               │
┌──────────────────────────────▼────────────────────────────────┐
│                  系统能力层（官方 API）                        │
│  @system.file / @system.storage / @service.health /            │
│  @system.velaclaw / @system.router / @system.prompt(待确认)     │
└────────────────────────────────────────────────────────────────┘
```

**双引擎路由策略（核心逻辑）：**

```
用户输入
  ├─ 命中功能映射表 → 功能直达
  ├─ 本地引擎检索（0.5~1s）
  │     ├─ 命中 → 精确答案卡片（标注"本地索引"）
  │     └─ 未命中/结果弱
  │           ├─ 联网可用 → velaclaw.ask(输入 + 本地候选依据) → AI 精炼答案
  │           └─ 断网 → 本地模糊匹配 + 相关词条推荐（离线兜底）
```

---

## 五、数据设计

### 5.1 知识库数据包（引擎数据格式）

沿用 v3.4 格式，由 `tools/splitData.html` 生成：

```
meta.txt           # JSON：totalCount、chunks、maps、bucketSize
block_*.txt|.bin   # 哈希桶 → ID 列表（索引）
map_*.txt          # ID → 展示数据（标题/年份/地区/分类）
detail_*.txt       # 完整内容（原因/影响等）
year_index.txt     # 年份 → ID 列表
```

多知识库 = 多份数据包（不同 dataPath），设置页切换。

**待办**：确认各知识库数据源（历史大事记已有；词典/百科/单位需采集或整理开放数据）。

### 5.2 健康日志数据

- 订阅 `service.health` 数据（HEART_RATE/SPO2/STRESS），按 `{timeStamp, value}` 追加落盘。
- 构造「日期/时段 → 数值序列」索引，支持"昨天心率趋势""凌晨压力"等时间检索。
- 生命周期：应用运行期采集，容量上限（如最近 7 天），防存储膨胀。

### 5.3 系统功能映射表

```
const FUNC_INDEX = {
  '番茄':   { type:'route', uri:'/pages/tomato' },
  '计时':   { type:'route', uri:'/pages/timer' },
  '呼吸':   { type:'route', uri:'/pages/breath' },
  '设置':   { type:'route', uri:'/pages/settings' },
  ...
}
```

---

## 六、AI 集成设计（velaclaw）

- **调用**：`velaclaw.ask({ query, success, fail })`，`res.reply` 为回复。
- **RAG 增强**：对本地未命中的查询，将本地引擎候选结果（标题/摘要）拼入 query 上文，让 AI 基于事实作答，降低幻觉。
- **降级**：`fail` 回调（1000 AI 不可用 / 204 超时）→ 提示"AI 暂不可用"，回退本地结果或给出本地相关词条。
- **配置**：goldfish 模拟器需在 NSH 终端启动 `ai_agent`，`set_llm https://token-plan-cn.xiaomimimo.com/v1 <model> tp-套餐KEY`（大赛 Token Plan）。
- **开关**：设置页提供 AI 开关，关闭后纯本地模式。

---

## 七、UI 设计（页面级）

| 页面 | 内容 | 关键组件 |
|---|---|---|
| 首页 `pages/index` | 搜索框（键盘/语音入口）+ 实时健康三卡（心率/血氧/压力，1Hz） | input、health 订阅 |
| 结果页 `pages/result` | 短答案卡片流，标注来源（本地/健康/AI） | 卡片列表 |
| 详情/操作页 `pages/detail` | 简短说明 + 功能触发按钮 | text、button |
| 健康日志页 `pages/healthlog` | 按日心率/压力趋势（简单折线/条形） | canvas/div 绘制 |
| 设置页 `pages/settings` | 知识库切换、AI 开关、索引统计、数据导入入口 | switch、list |

适配圆形表盘：`config.designWidth: 480`，`deviceTypeList: ["watch"]`。

---

## 八、性能承诺与展示口径

| 演示点 | 口径 |
|---|---|
| 检索速度 | "0.5~1 秒离线出结果"（不夸大） |
| 索引容量 | "全文索引 40 万+ 关键字，覆盖 18 万条记录" |
| 子串检索 | "搜一个'皇'字，命中全部相关词条" |
| 内存 | "引擎常驻内存 < 158KB" |
| 离线能力 | "无网环境下完整可用" |
| AI 对比 | "AI 负责理解，引擎负责精确与离线" |

---

## 九、演示脚本（60 秒，全模拟器可跑）

1. 断网演示：搜"圆周率" → 0.5~1 秒出值（标注"本地检索 · 离线可用"）
2. 精确性：搜"秦始皇" → 1 秒内出"公元前 221 年统一六国"
3. 子串能力：搜"皇" → 命中"秦始皇"等多条
4. 健康融合：搜"压力" → 实时压力值 + 科普卡 + 呼吸放松入口
5. 系统直达：搜"番茄" → 一键进入番茄钟
6. 容量展示：设置页"全文索引 40 万+ 关键字 · 内存 158KB · 完全离线"
7. AI 增强（联网时）："最近压力大怎么办" → AI 结合实时健康值与知识给出建议

---

## 十、技术可行性分析

### 10.1 已确认可行（有官方依据/现有代码）

| 技术点 | 依据 |
|---|---|
| 本地引擎 | SearchEngine.js 已用 `@system.file`/`@system.storage`，SplashPage.ux 集成验证 ✅ |
| service.health | 官方教程（接口、manifest、模拟器镜像、数据回放）✅ |
| velaclaw | 官方教程（API、CONFIG、MiMo 配置、错误码）✅ |
| 快应用 UI/路由 | 官方框架标准能力 ✅ |

### 10.2 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| AIoT IDE 模拟器不支持 velaclaw | AI 演示受限 | AI 在 openvela goldfish 模拟器验证（需 WSL 环境）；AIoT IDE 用于 UI/健康开发 |
| WSL/全量源码依赖重启系统 | 环境就绪延迟 | 用户先重启完成 WSL 安装；并行用 AIoT IDE 先行开发 UI 与引擎 |
| 语音识别能力未确认 | 语音输入不可用 | 键盘输入为主路径；语音列为可选增强，不阻塞核心功能 |
| 蓝牙导入模拟器不可用 | 导入演示受限 | 用 adb push 模拟导入流程；蓝牙作为真机链路写进说明文档（待对端联调） |
| 知识库数据源缺失 | 演示内容不足 | 历史大事记数据已有；词典/百科/单位用可开源数据生成 |
| 引擎性能在模拟器下降 | 演示变慢 | 以模拟器实测口径描述；模拟器演示控制数据量 |

### 10.3 环境依赖清单

| 环境 | 用途 | 状态 |
|---|---|---|
| AIoT IDE 1.99.3（D:\AIoT IDE\） | UI 开发 + 健康数据 + 引擎联调 | ✅ 已装，需更新插件至 1.7.22+ |
| WSL + Ubuntu | openvela 全量源码 + goldfish 模拟器（AI/velaclaw 验证） | ⏳ 已装功能，待重启系统完成 |
| repo 工具 | repo sync 拉取源码 | ⏳ WSL 内安装 |
| MiMo Token | AI 调用 | ⏳ 官方已发放/待配置 |

---

## 十一、开发里程碑（剩余约 40 天）

| 阶段 | 时间 | 交付物 |
|---|---|---|
| M1 环境就绪 | 第 1 周 | WSL 重启完成、repo sync 源码、goldfish 模拟器跑通 hello_quickapp |
| M2 引擎迁移 | 第 1-2 周 | 引擎 + 数据包在快应用跑通，生成历史知识库数据包 |
| M3 健康模块 | 第 2-3 周 | service.health 三卡 + 健康日志落盘检索 |
| M4 AI 集成 | 第 3-4 周 | velaclaw 调用 + 双引擎路由 + 降级 |
| M5 UI 打磨 | 第 4-5 周 | 完整页面、圆形表盘适配、交互优化 |
| M6 数据扩充 | 第 5 周 | 词典/百科/单位多知识库 |
| M7 集成验收 | 第 6 周 | 全流程演示脚本跑通、性能测试 |
| M8 参赛材料 | 第 7 周 | README、release.rpk、AI 日志、介绍文档、演示视频 |
| 截止 | 2026-09-20 | 提交作品 |

---

## 十二、参赛材料清单

1. 快应用源码工程（src/、manifest.json、package.json）
2. `release.rpk`（生产模式打包）
3. README 作品说明（替换组委会模板）
4. AI Coding 日志（logs/ 目录）
5. 作品介绍文档（docx/pdf/pptx）
6. 演示视频（≤5 分钟）
7. 数据包与数据生成工具说明

---

## 十三、附录：官方资源

- 快应用手动开发指南：docs 仓库 `zh-cn/contest_2026/quickapp/quickapp_manual.md`
- velaclaw 教程：`zh-cn/contest_2026/quickapp/quickapp_velaclaw.md`
- service.health 手册：`zh-cn/contest_2026/quickapp/service_health_guide.md`
- 大赛总览：`zh-cn/contest_2026/contest_overview.md`
- 代码提交指南：`zh-cn/contest_2026/code_submission_guide.md`
